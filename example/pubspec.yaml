name: example
description: "A new Flutter project."
publish_to: 'none'
version: 36.3.3

environment:
  sdk: '>=3.3.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  app_version_update:
    path: ../
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true
